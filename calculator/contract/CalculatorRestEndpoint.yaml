---
swagger: "2.0"
info:
  version: "1.0.0"
  title: "swagger definition for io.mysamples.bmi.CalculatorRestEndpoint"
  x-java-interface: "gen.swagger.CalculatorRestEndpointIntf"
basePath: "/"
consumes:
- "application/json"
produces:
- "application/json"
paths:
  /bmi:
    get:
      operationId: "calculate"
      parameters:
      - name: "height"
        in: "query"
        required: false
        type: "number"
        format: "double"
      - name: "weight"
        in: "query"
        required: false
        type: "number"
        format: "double"
      responses:
        200:
          description: "response of 200"
          schema:
            $ref: "#/definitions/BMIViewObject"
  /livecheck:
    get:
      operationId: "liveCheck"
      parameters: []
      responses:
        200:
          description: "response of 200"
          schema:
            type: "string"
  /sleeptest:
    get:
      operationId: "sleepTest"
      parameters: []
      responses:
        200:
          description: "response of 200"
definitions:
  BMIViewObject:
    type: "object"
    properties:
      result:
        type: "number"
        format: "double"
      instanceId:
        type: "string"
      callTime:
        type: "string"
    x-java-class: "io.mysamples.bmi.BMIViewObject"
